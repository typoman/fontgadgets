# In the fontemethod add undo if the environment is RF

# glyph
- scale

# features
getGlyphALternates
- filtered by feature tag
- observer for changes in the included feature files that makes the RFeatures dirty
